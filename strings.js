var myName = "Gen Rumancik";

var output = "";

for (var i = 11; i >= 0; i -=1) {
switch (myName[i]) {
	case "a":
	case "e":
	case "i":
	case "o":
	case "u":
	break;
	default:	

	output = output + myName[i]
  }
}
console.log(output)

var Dino = "Tyrannasaurus Rex"

var outpu = "";

for (var i = 16; i >= 0; i -=1) {
	switch (Dino[i]) {
		case "a":
		case "e":
		case "i":
		case "o":
		case "u":
		break;
		default:

		outpu = outpu + Dino[i]

	}
}
	console.log(outpu)